[project]
name = "ludo-telegram-bot"
version = "0.1.0"
description = "A Telegram bot starter for a Ludo game"
requires-python = ">=3.13"
dependencies = [
    "python-telegram-bot>=22.8",
]

[project.scripts]
ludo-bot = "ludo_bot.main:main"
